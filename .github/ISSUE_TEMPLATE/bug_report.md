# Bug Report

Thank you for reporting a bug! Please fill in as much as you can.

## Environment
- OS: (e.g. macOS 14 / Ubuntu 22.04 / Windows 11)
- Python version: (e.g. 3.11.9)
- Install method: (pkg / one-line script / git clone)
- Eidetic version: (`eidetic --version` or `python -m memory_server --version`)
- Embedding backend: (Ollama bge-m3 / llama.cpp / fts-only fallback)

## Description
What happened? What did you expect instead?

## Reproduction steps
1. ...
2. ...
3. ...

## Logs / output
Paste relevant output here. For service issues, include:
```bash
eidetic doctor            # integrity audit
# logs live under <install-dir>/logs/ (server.log / err.log)
```

## Additional context
Anything else that might help (config.yaml redacted, memory.db size, chunk count, etc.)
